const componentAnatomyEyeRight = (
  params = {
    hp: 10,
    ifDestroyed: "blind"
  }
) => {
  return {
    hp: params.hp,
    ifDestroyed: params.ifDestroyed
  };
};

export default componentAnatomyEyeRight;
