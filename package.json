{
  "name": "snail4",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "parcel src/index.html",
    "build": "parcel build src/index.html --public-url /snail4/",
    "test": "echo \"Error: no test specified\" && exit 1",
    "predeploy": "rm -rf dist && npm run build",
    "deploy": "gh-pages -d dist"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "lodash": "^4.17.15",
    "overprint": "^0.8.1"
  },
  "devDependencies": {
    "cssnano": "^4.1.10",
    "gh-pages": "^2.1.1"
  }
}
